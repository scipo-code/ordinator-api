[workspace]
resolver = "2"
members = [
    "scheduling_system",
    "shared_types",
    "imperium",
    "data_processing",
]

[profile.dev]
panic = 'abort'
debug = true

[profile.release]
debug = true
panic = 'abort'

