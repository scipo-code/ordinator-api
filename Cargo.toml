[workspace]
resolver = "2"
members = [
    "scheduling_system",
    "shared_types",
    "imperium",
    "data_processing",
]

[profile.dev]
panic = 'abort'
force-frame-pointers = "yes"
debug = true

[profile.release]
panic = 'abort'
