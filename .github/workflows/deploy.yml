name: Deploy to remote server
on:
  push:
    branches:
      - main
 
jobs:
  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Setup ssh key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/gbar
        chmod 600 ~/.ssh/gbar
        ssh-keyscan -H login1.gbar.dtu.dk >> ~/.ssh/known_hosts      
    
    - name: Deploy to server
      run: |
        ssh -t -i ~/.ssh/gbar cbrje@login1.gbar.dtu.dk << 'EOF'
          echo "${{ secrets.SECRET_KEY_PASSWORD }}"
          echo "${{ secrets.DTU_PASSWORD }}"
          cd /zhome/6b/5/127255/ordinator
          git pull main
          zellij -l ordinator.kdl
        EOF
